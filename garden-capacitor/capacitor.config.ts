import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.garden',
  appName: 'Garden Service Tracker',
  webDir: 'build',
  plugins: {
    // Add any Capacitor plugins configuration here
  },
};

export default config;